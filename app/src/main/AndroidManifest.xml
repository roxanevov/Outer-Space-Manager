<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vovard.com.outerspacemanager.outerspacemanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".activity.SingUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.VueGeneraleActivity" />
        <activity android:name=".activity.BuildingActivity"></activity>
        <activity android:name=".activity.BuildingDetailActivity"></activity>
        <activity android:name=".activity.ShipActivity"></activity>
        <activity android:name=".activity.FleetActivity"></activity>
        <activity android:name=".activity.GalaxieActivity"></activity>
        <activity android:name=".activity.SearchActivity"></activity>
    </application>

</manifest>